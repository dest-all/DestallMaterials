@if (Show)
{
    @ChildContent
}

@code {
    [Parameter]
    public bool Show { get; set; }

    [Parameter]
    [EditorRequired]
    public RenderFragment ChildContent { get; set; }

    public void Refresh(bool show)
    {
        Show = show;
        StateHasChanged();
    }
}
